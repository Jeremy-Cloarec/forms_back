'use strict';

/**
 * formulaire-formulaire3 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::formulaire-formulaire3.formulaire-formulaire3');
