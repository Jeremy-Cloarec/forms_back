'use strict';

/**
 * formulaire-formulaire1 controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::formulaire-formulaire1.formulaire-formulaire1');
