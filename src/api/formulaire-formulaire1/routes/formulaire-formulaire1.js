'use strict';

/**
 * formulaire-formulaire1 router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::formulaire-formulaire1.formulaire-formulaire1');
